# AI駆動フルスタック開発フレームワーク

Claude.ai で設計 → Claude Code で実装する、再現可能な開発手法。

---

## 全体像

```
Phase 0    Phase 1      Phase 2       Phase 3        Phase 4        Phase 5
コンセプト → 壁打ち →  設計仕様書 →  開発ドキュメント →  実装準備 →    実装
(人間)    (Claude.ai)  (Claude.ai)   (Claude.ai)    (Claude.ai)   (Claude Code)
          対話型       成果物型       成果物型        成果物型       コード生成
```

**原則: 後工程に渡すのは常にファイル。口頭の合意ではなくMarkdownに書き出す。**

---

## Phase 0: コンセプト（人間が用意する）

### ゴール

「何を作りたいか」を1ページで言語化する。完璧でなくていい。

### 使用テンプレート

`templates/concept.md`

### やること

- アイデアをメモ書きレベルで書く
- 以下が含まれていると後工程がスムーズ:
  - 誰のためのプロダクトか
  - どんな体験を提供するか
  - 自分がなぜ作りたいか（ポートフォリオ？事業？学習？）

### 成果物

`concept.md`

### Tips

- 構成が雑でも問題ない。Phase 1でClaude.aiが構造化してくれる
- 「コンセプトに自信がない」状態でもPhase 1に進んでいい。壁打ちで磨かれる

---

## Phase 1: 壁打ち（Claude.ai）

### ゴール

コンセプトを「画面とフローが見える」レベルまで具体化する。

### 進め方

Claude.ai の1チャットで対話的に進める。concept.mdを添付して開始。

### プロンプトテンプレート

```
添付のコンセプトをWebアプリケーション化したいです。
どのような画面が必要でどのようなフローになるか壁打ちをさせてください。
```

### 壁打ちで決めること

| 項目 | 質問例 |
|------|--------|
| **スコープ** | MVP？フル機能？ポートフォリオ？ |
| **ユーザーの種類** | ロールは何種類？権限の違いは？ |
| **コアフロー** | ユーザーが最も頻繁に行う操作は何？ |
| **画面一覧** | 何画面必要？各画面の目的は？ |
| **画面遷移** | 画面間の導線は？ |
| **データ構造** | 主要なエンティティは？関係性は？ |

### 成果物

チャット内で以下が合意された状態:

- 画面一覧（名称 + 目的 + 簡易説明）
- 画面遷移の全体像
- ロール定義
- コアフロー（最も重要なユーザー体験の流れ）

### Tips

- **1チャットに収める。** 壁打ちの文脈が途切れると議論が巻き戻る
- **選択式で聞き返してくれる**ので、直感で選んでいい。後で変えられる
- **「それで良いです」と言って先に進める**のが大事。完璧を求めると止まる

---

## Phase 2: 設計仕様書（Claude.ai）

### ゴール

Phase 1の合意を「設計仕様書」として書き出す。開発者（自分含む）が読んで画面が作れるレベル。

### 使用テンプレート

`templates/design-spec.md`

### 進め方

Phase 1と同じチャット、またはPhase 1の成果をまとめて新しいチャットに渡す。

### プロンプトテンプレート

```
ここまでの議論を設計仕様書としてMarkdownファイルにまとめてください。

含める内容:
1. プロダクト概要（コンセプト、方針、UI設計原則）
2. ユーザーロールとフロー
3. 画面一覧（全画面の名称・目的・レイアウト・認証要否）
4. コア画面の詳細仕様（各ステップで何が表示され、何が操作できるか）
5. データモデル（ER図 + 各テーブルのカラム定義）
6. 画面遷移図（Mermaid形式で別ファイル）
```

### 深掘りが必要な場合

```
[特定の画面名]の詳細仕様を詰めたいです。
- ユーザーが画面を開いたとき何が見えるか
- 操作可能なアクションは何か
- 各アクション後の状態遷移は何か
- レイアウト（左右分割？中央集中？グリッド？）
```

```
UIコンポーネントのレイアウトパターンを体系化したいです。
画面の目的別に最適なレイアウトを定義し、全画面にマッピングしてください。
```

### 成果物

| ファイル | 内容 |
|----------|------|
| `設計仕様書.md` | プロダクト設計の全体像 |
| `screen_flow.mermaid` | 画面遷移図 |

### Tips

- **仕様書は「長くていい」。** Phase 3以降でClaude.aiが参照するための情報源
- **データモデルはこの段階で固める。** 後から変えるとAPI設計から全部やり直し
- **Mermaid図は別ファイルにする。** 仕様書本文に埋め込むとClaude.aiが図を読み飛ばすことがある

---

## Phase 3: 開発ドキュメント（Claude.ai）

### ゴール

設計仕様書を「開発着手できる」レベルの技術ドキュメントに変換する。

### 使用テンプレート

PROJECT-PLAYBOOK のドキュメントテンプレート一式:

| テンプレート | 内容 |
|-------------|------|
| `01_prd.md` | 概要/背景と目的、ターゲットユーザーと課題、主要機能（ユーザーストーリー）、成功指標（KPI）、スコープ外 |
| `02-01_system-design-doc.md` | Goal/Non-Goal、アーキテクチャ概要、技術選定(ADR)、画面一覧、API設計、データモデル、セキュリティ・パフォーマンス、テスト戦略、モニタリング・ログ |
| `02-02_feature-design-doc.md` | 背景と目的、Goal/Non-Goal、影響範囲、実装アプローチ、詳細設計、テスト方針、ロールアウト計画 |
| `03_dev-setup.md` | 環境構築手順、必要なツール・アカウント、ローカル開発サーバー起動、テスト実行、ブランチ戦略、トラブルシューティング |
| `04_deployment-procedure.md` | 各環境へのデプロイ手順、リリース前チェックリスト、ロールバック手順、デプロイ後確認、緊急時連絡先 |
| `05_operation-runbook.md` | モニタリング・ログ設計、監視ポイントとアラート、よくある障害と対処法、ログ確認方法、定期メンテナンス |

### 進め方

新しいチャットを開始。設計仕様書 + PROJECT-PLAYBOOKテンプレート + 技術スタック参考資料（あれば）を添付。

### プロンプトテンプレート

```
添付の設計仕様書とドキュメントテンプレート（PROJECT-PLAYBOOK）を元に、
開発に着手できるようドキュメント一式を作成してください。

テンプレートの構成に従い、以下のドキュメントを埋めてください:
1. PRD（01_prd.md）
2. System Design Doc（02-01_system-design-doc.md）
3. Feature Design Doc（02-02_feature-design-doc.md）— テンプレートのまま残す
4. Dev Setup（03_dev-setup.md）
5. Deployment Procedure（04_deployment-procedure.md）
6. Operation Runbook（05_operation-runbook.md）

技術スタックの前提:
- フロントエンド: [Next.js / React / etc.]
- バックエンド: [NestJS / Express / etc.]
- DB: [PostgreSQL / MySQL / etc.]
- 認証: [NextAuth / Firebase Auth / etc.]
- ホスティング: [Cloud Run / Vercel / etc.]
- IaC: [Terraform / なし]
```

### 技術スタックが未定の場合

```
技術スタックをまだ決めていません。
ポートフォリオ用途で、[フロントはReact系、バックはTypeScript]という方向性だけあります。
選択肢を提示して一緒に決めたいです。
```

### 成果物

PROJECT-PLAYBOOKテンプレートに従った6ファイル。

### Tips

- **技術選定はこのフェーズで確定させる。** Phase 4以降で変えると手戻りが大きい
- **ADR（Architectural Decision Records）は必ず書く。** 「なぜこの技術を選んだか」がポートフォリオの差別化になる
- **API設計はリクエスト/レスポンスのボディまで書く。** バックエンド実装時のプロンプトで「API設計に従って」と指示できる
- **DBスキーマは実際のコード（Prisma/Drizzle等）で書く。** そのままコピペで使える状態にする

---

## Phase 4: 実装準備（Claude.ai）

### ゴール

Claude Codeに渡す2つのファイルを作る: コンテキストファイルと段階的プロンプト。

### 使用テンプレート

| テンプレート | 用途 |
|-------------|------|
| `templates/CLAUDE.md` | Claude Codeが自動読み込みするプロジェクトコンテキスト |
| `templates/claude-code-prompts.md` | ステップ別プロンプト集の構成ガイド |

### プロンプトテンプレート

```
ここからはClaude Codeで実装を進めようと思っています。
以下の2ファイルを作成してください。

1. CLAUDE.md — Claude Codeが自動で読むプロジェクトのコンテキストファイル
   - templates/CLAUDE.md の構成に従う
   - Tech Stack、ディレクトリ構成、設計方針、コマンド一覧、docsへの参照パス

2. claude-code-prompts.md — Claude Codeに1つずつ投げるステップ別プロンプト集
   - templates/claude-code-prompts.md の構成に従う
   - 各ステップに明確なゴール（「〜が動く状態」）
   - 依存関係順（骨格 → DB → API → 認証 → UI → インフラ → テスト）
   - 1ステップ = Claude Codeの1チャットで完結する粒度
```

### CLAUDE.md に書くこと / 書かないこと

| 書く | 書かない |
|------|---------|
| 確定した技術スタック | ステップ別の作業指示 |
| ディレクトリ構成 | 変わりやすい情報（TODO、進捗） |
| 重要な設計方針（3〜5個） | 冗長な説明 |
| 開発コマンド一覧 | Phase 2〜3の内容の複製 |
| docsへのファイルパス | |
| コーディング規約 | |

### ステッププロンプトの設計原則

| 原則 | 理由 |
|------|------|
| **1ステップ = 1チャット** | コンテキストウィンドウの溢れ防止 |
| **ゴールを「動作確認可能な状態」で定義** | Claude Codeが完了判断できる |
| **参照すべきdocsファイルを明示** | 必要な情報だけ読ませる |
| **「まだやらないこと」を明示** | スコープクリープ防止 |
| **依存関係順に並べる** | 前のステップの成果物が次の前提になる |

### 推奨ステップ構成（Webアプリの場合）

| Step | 内容 | 粒度の目安 |
|------|------|-----------|
| 1 | モノレポ骨格 + 開発環境 | フレームワーク初期化、Docker、.env |
| 2 | DBスキーマ + シード | テーブル定義、デモデータ |
| 3 | バックエンドAPI（CRUD） | 全エンドポイント、バリデーション |
| 4 | 認証 | OAuth、ガード、protected routes |
| 5 | 共通UIコンポーネント | レイアウト、デザインシステム |
| 6 | コア画面 | 最も重要な画面 |
| 7 | 残り画面 | 2nd priority の画面群 |
| 8 | インフラ + CI/CD | Terraform、GitHub Actions、Dockerfile |
| 9 | テスト + 仕上げ | Unit、E2E、Lint |

### 成果物

| ファイル | 配置場所 | 用途 |
|----------|----------|------|
| `CLAUDE.md` | リポジトリルート | Claude Codeが自動読み込み |
| `claude-code-prompts.md` | 手元に保持 | ステップ別プロンプト |

### Tips

- **Step 1〜2は1チャットにまとめてもOK。** 骨格 + スキーマは依存が近い
- **UI実装ステップが最も重い。** コア画面と残り画面は分ける
- **インフラは後回しで良い。** ローカルで動くものを先に作る

---

## Phase 5: 実装（Claude Code）

### ゴール

Claude Codeでプロンプト集を順に実行し、動くアプリケーションを完成させる。

### 進め方

```
リポジトリ初期化
├── CLAUDE.md を配置
├── docs/ にPhase 2〜3の成果物を配置
└── Claude Code を開く
    ├── Chat 1: Step 1（〜2）をコピペ → 動作確認
    ├── Chat 2: Step 3 をコピペ → 動作確認
    ├── Chat 3: Step 4 をコピペ → 動作確認
    ├── ...
    └── Chat N: 最終Step → 動作確認
```

### チャット分割の目安

| チャット | Steps | 目安ファイル数 |
|----------|-------|-------------|
| Chat 1 | 骨格 + DB | 〜20ファイル |
| Chat 2 | API | 〜30ファイル |
| Chat 3 | 認証 | 〜10ファイル |
| Chat 4 | コアUI | 〜20ファイル |
| Chat 5 | 残りUI | 〜20ファイル |
| Chat 6 | インフラ + テスト | 〜20ファイル |

### ステップ間の動作確認

各チャットの最後に必ず動作確認する:

| Step | 確認方法 |
|------|----------|
| 骨格 | `pnpm dev` で両方のサーバーが起動する |
| DB | `db:push` + `db:seed` + `db:studio` でデータが見える |
| API | `curl` で各エンドポイントが200を返す |
| 認証 | ブラウザでOAuthログインが通る |
| UI | ブラウザでコアフローが一通り動く |
| インフラ | `terraform plan` がエラーなく通る |
| テスト | `pnpm test` が全パス |

### 問題が起きたら

```
[エラーメッセージをコピペ]

上記のエラーが出ています。修正してください。
```

### Tips

- **新しいチャットではCLAUDE.mdが自動で読まれる**ので、プロジェクト説明は不要
- **前のチャットで作ったファイルはディスクに残っている**ので、引き継ぎ説明も不要
- **1ステップが大きすぎると感じたら分割する。** 完璧な粒度は事前にはわからない
- **生成されたコードは必ずレビューする。** 特にセキュリティ（認証ガード、バリデーション）

---

## フェーズ間の成果物フロー

```
Phase 0         Phase 1          Phase 2             Phase 3               Phase 4          Phase 5
concept.md → (チャット内合意) → 設計仕様書.md           → 01_prd.md            → CLAUDE.md     → コード
                              screen_flow.mermaid     02-01_system-design    prompts.md
                                                      02-02_feature-design
                                                      03_dev-setup
                                                      04_deployment
                                                      05_operation
```

**重要: 各Phaseの成果物は次のPhaseの入力になる。飛ばすと品質が落ちる。**

---

## テンプレート一覧

| テンプレート | Phase | 説明 |
|-------------|-------|------|
| `templates/p-0/concept.md` | 0 | コンセプトメモ |
| `templates/p-2/design-spec.md` | 2 | 設計仕様書 |
| `templates/p-4/CLAUDE.md` | 4 | Claude Code コンテキスト |
| `templates/p-4/claude-code-prompts.md` | 4 | ステップ別プロンプト集 |
| `templates/p-3/project-playbook`(PROJECT-PLAYBOOK テンプレート一式) | 3 | 開発ドキュメント |

---

## アンチパターン

| やりがちなこと | なぜダメか | 代わりにやること |
|--------------|-----------|---------------|
| コンセプトなしでいきなりClaude Codeに「アプリ作って」 | 方向性がブレて手戻り大量 | Phase 0〜4を踏む |
| 設計仕様書なしで開発ドキュメントを作る | API設計とUI設計が噛み合わない | Phase 2で画面仕様を固めてからPhase 3 |
| 全ステップを1つのClaude Codeチャットで実行 | コンテキスト溢れで品質低下 | ステップごとにチャットを分ける |
| CLAUDE.mdにステップ別指示を書く | 毎回読まれるので情報過多 | CLAUDE.md = 前提知識、prompts.md = 指示 |
| 技術スタックを実装フェーズで変更 | ドキュメント全体の書き直し | Phase 3で確定させる |
| 動作確認せずに次のステップへ進む | エラーが積み上がって修正困難 | 各ステップ後に必ず動作確認 |
